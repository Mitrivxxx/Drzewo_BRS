#include "pch.h"
#include "iostream"

using namespace std;
// siema jestem nowy
// pierwsza zmiana na dev


class BST {
private:
    // Struktura węzła drzewa
    struct Node {
        int data;
        Node* left;
        Node* right;

        Node(int value) : data(value), left(nullptr), right(nullptr) {}
    };
};

int main() {


    return 0;
}
